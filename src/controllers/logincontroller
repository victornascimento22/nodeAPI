'use strict'


exports.post = (req, res, next) => {
    res.status(200).send({
        username: username, // Use a variável username aqui
        password: password, // Use a variável password aqui
        body: req.body // Envie o corpo da solicitação corretamente
    });
};

export default controller;